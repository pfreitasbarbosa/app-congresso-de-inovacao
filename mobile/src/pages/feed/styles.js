import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    eventsContainer:{
        flexDirection: "column",
        alignItems: "center",
    },
    cardContainer:{
        paddingBottom: "25%",
    }
});